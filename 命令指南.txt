  `npm start`
启动开发服务器。

  `npm run build`
将您的网站捆绑到静态文件中进行生产。

  `npm run serve`
为本地构建的网站提供服务。

  `npm deploy`
将网站发布到GitHub页面。

我们建议您首先键入：
  `cd dgoeee-website`
  `npm start`


☆☆☆☆☆文件移动之后，删除 node_modules 文件夹，然后执行 npm install，即可正常启动项目
